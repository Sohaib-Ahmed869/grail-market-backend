# ── required ────────────────────────────────────────────────────────────────
# Where the vision service is. Must include the scheme.
VISION_URL=http://localhost:8100
PORT=8180

# ── pricing sources ─────────────────────────────────────────────────────────
# Pokemon sold comps, per grade. English sets only.
#
# Keys normally live ENCRYPTED in the provider_keys table, not here:
#   npm run keys -- --add <key> --label "which account"
#   npm run keys -- --list
#   npm run keys -- --import-env      (move an existing PPT_API_KEY into the store)
#
# Anything left here is ADDED to that pool as a fallback — handy on a local box
# or when the store is unreachable, but it should not be the source of truth.
# Each key keeps its OWN budget and breaker, so one running out does not
# disable the rest, and the pool drains the fullest key first.
PPT_API_KEY=

# Master secret for the encrypted key pool (AES-256-GCM). Generate with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# BACK THIS UP. Without it every key in provider_keys is unrecoverable. It
# protects the keys in a database dump or a stolen backup; it does not protect
# them from anyone who can read this file.
PPT_KEY_SECRET=

# Cards per request for the refresh job. The provider bills PER CARD RETURNED,
# so this is literally the price of a lookup. The scan path asks for 3 because
# it searches by fuzzy text and has to pick the right card out of the results;
# the refresh job already knows the exact name, set and number, so it asks for
# 1 and pays a third as much. Raise it only if ingest starts missing cards.
PPT_INGEST_PAGE_SIZE=1

# Live listings, and the marketplace-deletion compliance endpoint.
EBAY_APP_ID=
EBAY_CERT_ID=
EBAY_DEV_ID=
EBAY_VERIFICATION_TOKEN=
EBAY_ENDPOINT_URL=

# Names a card the catalogues cannot, and reads a printing off the artwork.
GEMINI_API_KEY=

# Optional catalogues.
JUSTTCG_API_KEY=
APITCG_API_KEY=
CARDGRADER_API_KEY=
# CardGrader bills ~$0.20/call and is OFF unless this is a positive number.
# It is a graded-price backup only; without it an unpriced card falls through
# to live asks, which is real data. See src/scans/cardgrader.ts.
CARDGRADER_DAILY_MAX=0

# ── storage ─────────────────────────────────────────────────────────────────
# Shared price cache. Without it the cache is local-only and per-process.
DATABASE_URL=

# ── tuning ──────────────────────────────────────────────────────────────────
# Cap the longest edge of an uploaded image. 0 = off, and off is correct:
# shrinking to 2000px once lost a collector number and mispriced a card by 30x.
MAX_INPUT_PX=0

# --- sessions ----------------------------------------------------------------
# Signs every session token. Without it /auth refuses to mint one, which is
# deliberate: a server that invents a key at boot signs everyone out on every
# restart. Changing it invalidates every session in existence.
#   node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
AUTH_SECRET=

# --- admin console -----------------------------------------------------------
# The console authenticates with a member's own session token; the `role`
# column on `users` decides what opens. There is no separate admin credential.
#
# ADMIN_OWNERS promotes existing accounts to owner at boot, so the first person
# in is not a hand-written UPDATE against production. Comma-separated emails.
# It only ever grants owner to an account that already exists, and never
# demotes anyone.
ADMIN_OWNERS=

# Development only, and ignored when NODE_ENV=production. With no sign-in
# screen yet, this stands in for one: requests with no token are treated as an
# operator of this name holding ADMIN_DEV_ROLE. Nothing is read from or written
# to `users`. Unset it and the console answers 401.
ADMIN_DEV_USER=
# tier-1 | tier-2 | moderator | trust-safety | owner   (default: owner)
ADMIN_DEV_ROLE=owner
